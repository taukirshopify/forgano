{%- style -%}
  .newsletter-{{ section.id }} {
    background-color: {{ section.settings.color_background }};
    color: {{ section.settings.color_text }};
  }

  .newsletter-{{ section.id }} .newsletter__input-group input[type=email]::-webkit-input-placeholder { color: {{ section.settings.plc_color }}; }
  .newsletter-{{ section.id }} .newsletter__input-group input[type=email]::-moz-placeholder { color: {{ section.settings.plc_color }}; }
  .newsletter-{{ section.id }} .newsletter__input-group input[type=email]::-ms-input-placeholder { color: {{ section.settings.plc_color }}; }
  .newsletter-{{ section.id }} .newsletter__input-group input[type=email] {
    background: {{section.settings.input_bg}};
    color: {{section.settings.input_text}};
  }
  .newsletter-{{ section.id }} .newsletter__input-group button.btn {
    background: {{section.settings.btn_bg}};
    color: {{section.settings.btn_text_color}};
  }
  .newsletter-{{ section.id }} .newsletter__input-group {
    border: 2px solid {{section.settings.border_color}};
  }
{%- endstyle -%}

<div class="newsletter-section newsletter-{{ section.id }}{% if section.settings.color_background == settings.color_body_bg %} newsletter-section--with-divider{% endif %}">
  <div class="page-width text-center">
    {%- for block in section.blocks -%}
      <div class="theme-block" {{ block.shopify_attributes }}>
        {%- case block.type -%}
          {%- when '@app' -%}
            {% render block %}
          {%- when 'title' -%}
            {%- if block.settings.title != blank -%}
              <p class="h3">{{ block.settings.title | escape }}</p>
            {%- endif -%}
          {%- when 'text' -%}
            {%- if block.settings.text != blank -%}
              <div class="rte">{{ block.settings.text }}</div>
            {%- endif -%}
          {%- when 'form' -%}
             <div class=" template-index newsletter__input-grouP">
              <div class="klaviyo-form-W4bApW"></div>
            </div>
          

           {% comment %} {%- if template.name == "index" or template.name == 'product'  -%}
            {%- render 'signup-offer-form', section_id: section.id, snippet_context: 'section', btn_text: section.settings.btn_text, placeholder: section.settings.placeholder -%}
             {%- else -%}
             {%- render 'newsletter-form', section_id: section.id, snippet_context: 'section', btn_text: section.settings.btn_text, placeholder: section.settings.placeholder -%}
             {%- endif -%} {% endcomment %}

          {%- when 'share_buttons' -%}
            {%- render 'social-sharing' -%}
        {%- endcase -%}
      </div>
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.newsletter.name",
  "max_blocks": 6,
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "title",
      "name": "t:sections.newsletter.blocks.title.name",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.newsletter.blocks.title.settings.title.label",
          "default": "Sign up and save"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.newsletter.blocks.text.name",
      "settings": [
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.newsletter.blocks.text.settings.text.label",
          "default": "<p>Subscribe to get special offers, free giveaways, and once-in-a-lifetime deals.</p>"
        }
      ]
    },
    {
      "type": "form",
      "name": "t:sections.newsletter.blocks.form.name",
      "limit": 1
    },
    {
      "type": "share_buttons",
      "name": "t:sections.newsletter.blocks.share_buttons.name",
      "limit": 1
    }
  ],
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.newsletter.settings.content"
    },
    {
      "type": "color",
      "id": "color_background",
      "label": "t:sections.newsletter.settings.color_background.label",
      "default": "#f9f9f9"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "t:sections.newsletter.settings.color_text.label",
      "default": "#1c1d1d"
    },
    {
      "type": "header",
      "content": "Signup Form Settings"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border Color",
      "default": "#FFFFFF"
    },
    {
      "type": "text",
      "id": "btn_text",
      "label": "Button Text"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Input Placeholder"
    },
    {
      "type": "color",
      "id": "btn_text_color",
      "label": "Button Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "btn_bg",
      "label": "Button Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "input_bg",
      "label": "Input Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "input_text",
      "label": "Input Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "plc_color",
      "label": "Placeholder Text Color",
      "default": "#000000"
    }
  ],
  "presets": [
    {
      "name": "t:sections.newsletter.presets.email_signup.name",
      "blocks": [
        {
          "type": "title"
        },
        {
          "type": "text"
        },
        {
          "type": "form"
        }
      ]
    }
  ]
}
{% endschema %}



<style>
 .newsletter-section .template-index.newsletter__input-grouP {
    width: 590px;
    margin: 0 auto;
    border: 2px solid #FFF;
}


@media only screen and (max-width: 700px) and (min-width: 100px)  {
  .newsletter-section .template-index.newsletter__input-grouP {
    width: auto;
  
  }



}
.newsletter-section button{
  font-family: montserratregular !important;
  letter-spacing: 3px !important;
  transition: .5s!important;
}
.newsletter-section button:hover{
  background: var(--colorBtnPrimaryLight) !important;
}
.newsletter-section input#email_82109582 {
  font-family: montserratregular !important;
}
.newsletter-section
input[type="email"]::placeholder {
             
  /* Firefox, Chrome, Opera */
  text-align: center;
  letter-spacing: 2px !important;
}
.newsletter-section
input[type="text"]::placeholder {
   
  /* Firefox, Chrome, Opera */
  text-align: center;
  letter-spacing: 2px !important;
}
.newsletter-section
input[type="tel"]::placeholder {
   
  /* Firefox, Chrome, Opera */
  text-align: center;
  letter-spacing: 2px !important;
}
.newsletter-section
input[type="email"]:-ms-input-placeholder {
   
  /* Internet Explorer 10-11 */
  text-align: center;
  letter-spacing: 2px !important;
}
.newsletter-section
input[type="email"]::-ms-input-placeholder {
   
  /* Microsoft Edge */
  text-align: center;
  letter-spacing: 2px !important;
}
</style>